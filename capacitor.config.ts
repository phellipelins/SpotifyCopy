import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.phellipelins.sptfycpy',
  appName: 'SpotifyCopy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
